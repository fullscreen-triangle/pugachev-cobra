{
  "name": "pugachev-cobra",
  "version": "0.1.0",
  "private": true,
  "description": "Media Effect Encoder — behavioural isomorphism DSL for video advertisement composition",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web",
    "start": "npm run start --workspace=web",
    "lint": "npm run lint --workspace=web",
    "validate": "python src/advalidation/run_all.py",
    "studio": "remotion studio remotion/index.ts",
    "render": "remotion render remotion/index.ts"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "devDependencies": {
    "@remotion/cli": "^4.0.477"
  }
}
